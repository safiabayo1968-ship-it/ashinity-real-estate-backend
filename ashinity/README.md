# Temporal Ashinity Real Estate Backend

FastAPI + PostgreSQL backend aligned to common real estate API endpoints:
- Auth: register, login (JWT)
- Users: get current user
- Properties: CRUD, filters, pagination
- Inquiries: create and list
- Favorites: add/list/remove

## Setup

1. Create `.env`:
